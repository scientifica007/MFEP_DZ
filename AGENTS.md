# MFEP_DZ — تعليمات وكلاء الذكاء الاصطناعي

هذا الملف هو نقطة الدخول المختصرة لأي Agent أو نموذج ذكاء اصطناعي يعمل على المستودع، بصرف النظر عن المزود أو النموذج أو الأداة.

## القاعدة الأساسية

**المستودع هو ذاكرة المشروع الدائمة. المحادثة، ذاكرة النموذج، أو تعليمات مزود بعينه ليست مصدرا مرجعيا للمشروع.**

قبل أي عمل جوهري اقرأ بالترتيب:

1. `PROJECT_MEMORY.md`
2. `ai/AGENT_CONTRACT.md`
3. `ai/PROJECT_CONTEXT.md`
4. الوثيقة المنهجية ذات الصلة في `docs/`
5. `ai/HANDOFF.md` لمعرفة حالة العمل الحالية

## قواعد إلزامية

- المصدر القانوني الرسمي يسبق استنتاجات النموذج.
- لا تُنشئ أو تغيّر حالة قانونية (`active`, `repealed`, ...) بلا evidence قابل للتدقيق.
- لا تُنشئ علاقة قانونية (`amends`, `repeals`, `implements`, ...) من التشابه اللغوي وحده.
- العربية والفرنسية مساران متقابلان للتحقق؛ عند فساد الاستخراج العربي ارجع إلى الفرنسية ثم تحقق من العربية المرئية في المواضع الحرجة.
- لا تعامل الإنجليزية بديلا عن المصدر الجزائري الرسمي؛ تستخدم طبقة مساعدة أو كلغة أصلية لمعاهدة عندما يثبت ذلك.
- افصل دائما بين `raw`, `normalized`, و`interpreted`.
- لا تستبدل قيمة قانونية خام بصمت. احتفظ بالقيمة الأصلية وسبب التطبيع.
- عبّر عن عدم اليقين صراحة. `unknown` أفضل من تخمين واثق.
- المخرجات الآلية ليست تلقائيا بيانات موثوقة. يجب أن تمر بمراحل التحقق المحددة في المشروع.
- لا تجعل corpus أو ontology أو schema تابعا لـ OpenAI أو Anthropic أو Google أو أي مزود آخر.
- **لا تعتبر فشل رابط PDF مباشر في JORADP دليلا على غياب المصدر.** هوية المصدر هي السنة + رقم الجريدة + اللغة؛ استخدم `scripts/joradp_resolver.py` قبل إعلان تعذر الوصول.
- لا تحذف recorded URL عند إيجاد رابط عامل آخر؛ احتفظ بالاثنين، لأن الأول provenance والثاني endpoint تشغيلي.
- لا تدخل PDF أو صور الجريدة الرسمية إلى Git مهما كان مصدرها؛ التنزيل، إن لزم، مؤقت فقط.

## الوصول إلى JORADP

عند الحاجة إلى PDF رسمي:

1. اقرأ `sources/sources.yml`؛
2. إن كان الرابط المباشر يعمل، استخدمه مع التحقق من هوية العدد؛
3. إن فشل، شغّل/طبق منطق `scripts/joradp_resolver.py`؛
4. اسمح باختلافات حالة الأحرف والامتداد؛
5. استخدم صفحة السنة الرسمية `ZAyyyy` أو `ZFyyyy` كـfallback؛
6. سجل `resolved_url`, طريقة الحل وتاريخ الفحص عند إضافة هذه المعرفة للحزمة؛
7. تحقق بعد الوصول من السنة، رقم العدد، اللغة والصفحات، لأن حل الرابط لا يساوي التحقق القانوني.

راجع `docs/JORADP_URL_RESOLUTION.md`.

## الكتابة في المستودع

عند إدخال معرفة قانونية جديدة:

1. احفظ المصدر وprovenance.
2. احفظ الدليل على العلاقات والحالة القانونية.
3. استخدم المعرفات والـvocabulary المعتمدة أو سجل أن القيمة مؤقتة.
4. حدث سجل القرار أو الذاكرة إذا نتج عن العمل قرار منهجي جديد.
5. حدث `ai/HANDOFF.md` إذا تغيّرت مرحلة العمل أو ظهرت مشكلة مفتوحة مهمة.

## طبقة الذكاء الاصطناعي

كل مواصفات العمل العامة توجد تحت `ai/`:

- `ai/AGENT_CONTRACT.md`: عقد السلوك الموحد.
- `ai/PROJECT_CONTEXT.md`: سياق مختصر للنموذج.
- `ai/context/`: سياق مقروء آليا.
- `ai/tasks/`: تعريفات المهام القياسية.
- `ai/evals/`: اختبارات قبول النماذج والتغييرات.
- `ai/adapters/`: قواعد عزل مزودي النماذج عند بناء الأتمتة.

إذا تعارضت تعليمات خاصة بنموذج مع هذه القواعد، لا يجوز لها تغيير الحقيقة القانونية أو منهج المشروع بصمت.
