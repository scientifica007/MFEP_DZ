# سجل تغييرات المعرفة والمنهج

هذا الملف يسجل التغييرات التي تؤثر في طريقة فهم المشروع أو بياناته أو تقييمها. لا يغني عن Git history؛ بل يشرح **لماذا** تغيرت المعرفة أو القاعدة.

## 2026-08-25

### تأسيس الذاكرة القانونية

- اعتماد المستودع ذاكرة دائمة للمشروع.
- اعتماد النص القانوني/التنظيمي/الإداري وحدة المعرفة الأساسية بدلا من ملف PDF.
- اعتماد نموذج متعدد الأبعاد للنظام القانوني الجزائري بدلا من سلم أسماء خطي فقط.
- اعتماد المقابلة العربية/الفرنسية مع الرجوع إلى الفرنسية عند فساد الاستخراج العربي والتحقق من العربية بصريا في المواضع الحرجة.
- اعتماد الإنجليزية طبقة مساعدة لا بديلا عن المصدر الجزائري الرسمي.
- اعتماد evidence و`status_as_of` شرطين لتحديد الحالة القانونية.
- اعتماد حفظ العلاقات القانونية مع موضع الدليل.
- اعتماد seed corpus ثم Graph Traversal بدلا من المسح العشوائي الشامل.

### تأسيس طبقة الذكاء الاصطناعي المستقلة

- اعتماد المشروع `Model-Agnostic` و`Provider-Agnostic`.
- تثبيت `AGENTS.md` نقطة دخول عامة لأي Agent.
- تثبيت `ai/AGENT_CONTRACT.md` عقدا موحدا للسلوك القانوني والبياني.
- إضافة `ai/PROJECT_CONTEXT.md` وmachine-readable context تحت `ai/context/`.
- إضافة `ai/HANDOFF.md` لاستئناف العمل دون الاعتماد على المحادثات.
- اعتماد الفصل بين `raw`, `normalized`, `interpreted` وبين مراحل التحقق.
- اعتماد task contracts تحت `ai/tasks/` بدلا من prompts الحرة فقط.
- اعتماد eval gate قبل السماح لنموذج أو prompt أو pipeline جديد بالعمل ضمن trusted workflow.
- تحديد أخطاء مانعة للاعتماد، خاصة false repeal/false amendment/hallucinated citation وفقدان provenance.
- اعتماد provider adapters لعزل تفاصيل APIs عن المنطق القانوني.
- اعتماد أن prompts لا تحمل الحقيقة القانونية المتغيرة؛ الحقيقة تبقى في corpus/metadata والمصادر.

### Seed Corpus 001 — بدء الاختبار على النصوص الحقيقية

- إنشاء `corpus/staging/seed-001/` وإدخال 10 سجلات رسمية مترابطة: القانون 08-07، القانون 18-10، المراسيم التنفيذية 12-125 و14-140 و16-282 و18-162 و20-340 و26-96، وقرارا 24 مارس 2022 و15 فبراير 2026 المتعلقان بتكوين المستفيدين من منحة البطالة.
- إنشاء الفهرس الآلي `metadata/staging/seed-001.jsonl`.
- إنشاء أول Graph عالي الثقة `graph/staging/seed-001-relations.jsonl`.
- إنشاء `metadata/discovery-queue.jsonl` لحفظ التبعيات المكتشفة غير المدخلة بدلا من اختراع بياناتها.
- تثبيت علاقة إلغاء صريحة: القانون 18-10 يلغي القانون 81-07 بموجب المادة 76، مع بقاء النصوص التطبيقية القديمة انتقاليا إلى غاية صدور نصوص تطبيقية جديدة حسب المادة نفسها.
- تثبيت علاقة إلغاء صريحة: قرار 15 فبراير 2026 يلغي قرار 24 مارس 2022 بموجب المادة 13.
- تثبيت علاقة تعديل: المرسوم التنفيذي 20-340 يعدل ويتمم المرسوم التنفيذي 18-162.
- تثبيت علاقات تطبيق مباشرة: 12-125 ← المادة 14 من القانون 08-07؛ 16-282 ← المادة 19؛ 18-162 ← المادة 15؛ و26-96 يطبق المادة 3 من 12-125 لإنشاء ثمانية معاهد.
- إبقاء غالبية الحالات القانونية `unknown` في staging عندما لم ينجز مسح كامل للنصوص اللاحقة، حتى لو كان النص مستشهدا به في 2026.

### نتائج تصميمية مستخرجة من Seed Corpus 001

- إلغاء حقل الصفحات الموحد عمليا لصالح `publication.locators.ar` و`publication.locators.fr` لأن ترقيم الصفحات يختلف بين العربية والفرنسية في عدد من الحالات الحقيقية.
- اعتماد اتجاه مؤقت لمعرفات النصوص المرقمة دون تضمين القطاع (`DZ-DE-2012-125`) لأن الانتماء القطاعي تصنيف وليس جزءا مستقرا بالضرورة من هوية النص.
- اعتماد معرفات مؤقتة للأعمال غير المرقمة مبنية على التاريخ + discriminator، دون اختراع أرقام غير رسمية.
- السماح بعلاقة Graph إلى هدف لم يدخل corpus بعد إذا وسم صراحة `unresolved` وأضيف إلى discovery queue.
- فصل التحقق من العلاقة القانونية عن التحقق من الحالة القانونية الحالية للنص.
- توثيق الفرق بين `status.as_of` و`effect.repeal_date`.
- ظهور حاجة Schema v1 إلى تمثيل صارم لحالات `partial verification`.

### Gold Evals الأولى

- إنشاء أربع حالات Gold مصدرية لاختبار استخراج العلاقات القانونية.
- الحالات تغطي: إلغاء القانون 81-07 بواسطة 18-10، تعديل 18-162 بواسطة 20-340، تطبيق 12-125 للمادة 14 من 08-07، وإلغاء قرار 24 مارس 2022 بواسطة قرار 15 فبراير 2026.
- حالة القانون 18-10 تشترط التقاط الحكم الانتقالي المتعلق باستمرار النصوص التطبيقية القديمة، لا مجرد تصنيف `repeals`.

### Seed 002 — حل أول التبعيات

- إدخال `DZ-LAW-1981-007` كسجل تاريخي في staging، مع إثبات حالته `repealed` من المادة 76 للقانون 18-10 وعدم مساواة `status.as_of` بتاريخ أثر الإلغاء.
- إدخال `DZ-DE-2022-070` كسجل أفقي مؤثر مباشرة في قطاع التكوين رغم صدوره عن قطاع العمل.
- تثبيت أن المادة 5 من 22-70 تنظم إحالة المستفيد من منحة البطالة إلى التكوين وتنص على تحديد كيفيات التطبيق بقرار وزاري مشترك مع قطاع التكوين والتعليم المهنيين.
- تثبيت أن المرسوم 22-70 في حالة `amended` على الأقل في 2026، إذ يعدله ويتممه المرسوم التنفيذي 26-87 المؤرخ في 21 جانفي 2026.
- تحديث Graph بحيث أصبحت العقدتان 81-07 و22-70 `staging_resolved` بدل `unresolved_seed_dependency`.
- توسيع discovery queue بسلسلة تعديلات 81-07، المرسوم 81-392 والنص الانتقالي لسنة 2020، وسلسلة تعديلات 22-70.

### تطور الـOntology بعد البيانات الحقيقية

- رفع `ontology/core.yml` إلى `0.2-draft`.
- توحيد المفتاح الإنجليزي للقرار الوزاري المشترك على `interministerial_order`، مع إبقاء `joint ministerial order` alias لغويا فقط.
- إضافة controlled vocabulary فعلي لـ`legal_function`.
- إضافة `targeted_population` و`specific_entities` إلى `scope` بدل القيم المركبة الحرة.
- إضافة العلاقة `provides_for_implementing_act` لتمييز النص التنظيمي الذي ينص على صدور أداة تطبيق أدنى عن `enabling_legislation` الذي يخص التشريع الممكّن.
- توحيد سجلات Seed والفهارس مع هذه المفردات.

### حاجز التحقق الحتمي قبل Schema v1

- إضافة `scripts/validate_repository.py` دون أي dependency خارجية أو استخدام للذكاء الاصطناعي.
- الإصدار الحالي يفحص سلامة JSONL، تكرار معرفات النصوص والحواف، وجود مسارات السجلات، مطابقة `legal_form` و`legal_status` وأنواع العلاقات مع `ontology/core.yml`، وسم Graph refs غير المحلولة، حالات discovery queue، وتطابق Eval cases مع expected outputs.
- توثيق الأداة في `scripts/README.md`.
- اعتماد المبدأ أن النجاح البنيوي في validator لا يساوي صحة الحكم القانوني؛ يبقى التحقق المصدري والدلالي وAR/FR والمراجعة القانونية طبقات مستقلة.
- تم اختبار Syntax الأداة في بيئة Python محلية. تعذر تشغيل clone كامل من GitHub داخل بيئة التنفيذ الحالية بسبب عدم توفر DNS إلى `github.com`، لذلك لم يسجل تشغيل end-to-end كنجاح حتى الآن.

### اعتماد Corpus نصي بلا PDF

- اعتماد قرار معماري دائم بعدم حفظ ملفات PDF أو صور الجريدة الرسمية داخل Git مهما اتسع corpus.
- إضافة `.gitignore` لمنع `*.pdf` و`*.PDF`.
- تحديث `scripts/validate_repository.py` بحيث يعد وجود PDF داخل شجرة المستودع خطأ validation.
- إضافة `docs/STORAGE_POLICY.md` لتوثيق أن JORADP يبقى مصدر binary خارجي، بينما يحفظ المشروع الروابط والمحددات والنصوص الخفيفة فقط.
- إضافة `scripts/materialize_joradp_text.py` الذي ينزل PDF إلى مساحة مؤقتة، يستخرج النص بواسطة `pdftotext`، ثم يحذف الملف الثنائي تلقائيا.
- التحقق من شجرة المستودع بعد التنفيذ لم يظهر أي ملف PDF مخزن.

### اعتماد حزمة Human + Machine لكل نص

- إضافة `docs/HUMAN_MACHINE_CORPUS.md` وتثبيت بنية موحدة لكل كيان قانوني تحت `corpus/texts/<TEXT_ID>/`.
- الواجهة البشرية هي `README.md`؛ سجل الآلة هو `record.yml`؛ الاستدعاء على مستوى المادة عبر `data/articles.jsonl`؛ حالة النص العربي/الفرنسي في `text/`; وإثبات المصدر في `sources/sources.yml`.
- اعتماد حالات صريحة للمتن: `source_resolution_pending`, `source_locator_only`, `transcription_pending`, `transcribed`, `verified`.
- لا يجوز وصف المتن بأنه مخزن داخليا وموثوق ما لم تصل transcription إلى `verified`.
- إنشاء `corpus/INDEX.md` نقطة دخول بشرية للمجموعة.

### هجرة السجلات الـ12 إلى البنية الدائمة

- هجرة جميع السجلات القانونية الـ12 الحالية إلى `corpus/texts/`.
- إعادة استخدام محتوى سجلات YAML الأصلية كـ`record.yml` عند النقل للحفاظ على provenance والعلاقات دون إعادة صياغة صامتة.
- إنشاء README بشري لكل سجل، وملفات لغة ذات حالة معلنة، وsource manifest، وفهرس مواد أولي أو كامل بحسب ما تحقق حتى الآن.
- القانون 08-07 لديه فهرس 32 مادة كاملا من حيث المعرفات والموضوعات وpage locators، مع بقاء text payload في `transcription_pending`.
- تحديث `metadata/staging/*.jsonl` للإشارة إلى `record.yml` و`human_view` و`articles_index` في المسارات الدائمة.
- حذف ملفات record القديمة من `corpus/staging/seed-*` بعد الهجرة، بحيث لا توجد نسختان دائمتان للحقيقة نفسها.
- بقاء وصف السجلات `staging` يعني مستوى التحقق، وليس أنها ما زالت مخزنة في مجلد staging.

## طريقة الاستخدام لاحقا

أضف مدخلا عند:

- تصحيح معلومة قانونية موثقة؛
- تغيير ontology أو schema مؤثر؛
- تغيير سياسة مصدر أو لغة؛
- تعديل gold output في evals؛
- تغيير قاعدة تحديد الحالة القانونية؛
- اكتشاف failure mode متكرر يستدعي تغيير المنهج؛
- تغيير سياسة التخزين أو حزمة Human + Machine؛
- اعتماد transcription كاملة أو تصحيحها على مستوى النص أو المادة.
